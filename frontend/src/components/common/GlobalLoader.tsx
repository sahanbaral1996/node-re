import * as React from 'react';
import { Logo } from 'assets/images';

const GlobalLoader: React.FunctionComponent = () => (
  <div className="global-loader__container">
    <Logo />
  </div>
);

export default GlobalLoader;
